/**
 * Takes a string of words separated by ', ' (comma space),
 * and returns an array of strings
 *
 * @param {string} str - a string of words separated by ', ' (comma space)
 *
 * @returns string[]
 */

export const stringToArray = (str) => {};
